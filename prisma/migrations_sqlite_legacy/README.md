# Legacy SQLite migration history archived after PostgreSQL baseline normalization.
# These files are intentionally kept out of prisma/migrations so Prisma Migrate
# only sees the new PostgreSQL baseline and subsequent PostgreSQL migrations.
