generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Consorcio {
  id        Int      @id @default(autoincrement())
  nombre    String
  direccion String
  createdAt DateTime @default(now())
}
